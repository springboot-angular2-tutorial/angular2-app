import {StyleSheet} from "aphrodite";

export const styles = StyleSheet.create({
  aside: {
    marginBottom: '8px',
    position: 'relative',
  },
  followBtn: {
    position: 'absolute',
    top: 0,
    right: 0,
    marginRight: '16px',
    marginTop: '8px',
  }
});
